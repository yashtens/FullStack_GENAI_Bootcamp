[project]
name = "full-stack-genai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yash marathe", email = "maratheyash58@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
full-stack-genai = "full_stack_genai:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
